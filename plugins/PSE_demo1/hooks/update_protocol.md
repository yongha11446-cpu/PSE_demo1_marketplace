[⚠️ 스트림/조건 데이터 감지 — 갱신 프로토콜 (자동 주입)]

방금 새 스트림/조건 값이 들어왔습니다. 계산·검증을 마친 뒤 아래 2곳을 **반드시 갱신**하세요.
옛 값은 남기지 말고 새 값으로 덮어쓸 것 (anti-stale):

1) sections/ 해당 공정 파일의 스트림·운전조건 → 새 값으로 갱신
2) `/업데이트 <파트>` (예: `/업데이트 탈수`) → 연구노트(연구노트.md)에 [날짜] 무엇이 어떻게 바뀌었는지 + 근거(논문·Table·쪽) 기록
   (같은 항목은 덮어쓰기, 새 항목은 추가. 글로벌 마스터_연구노트.md에도 반영)

원칙: 검증은 **원본 PDF를 직접 대조**, md를 그대로 믿지 말 것. 근거 없는 값은 "확인 필요"로 표시.
(이 안내는 연구 폴더에서만 의미가 있습니다. 비연구 작업이면 무시하세요.)
